<?php $html = file_get_contents(SITE_PATH . 'js/scrollTop.js') ?>

<div class="typography">
	<p>Generates a 'to top' button and binds an event which scrolls the user to the top of the page on click.</p>
</div>
<pre class="rainbow-pre"><code data-language="js"><?php echo htmlentities($html) ?></code></pre>
