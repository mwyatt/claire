<?php $html = file_get_contents(SITE_PATH . 'js/smoothScroll.js') ?>

<div class="typography">
	<p>Smoothly scrolls the user when clicking links with the correct class assigned to them.</p>
</div>
<pre class="rainbow-pre"><code data-language="js"><?php echo htmlentities($html) ?></code></pre>
